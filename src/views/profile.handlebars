<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Users</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous" />
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Nunito', sans-serif;
            background-color: #F0F8F4;
        }

        h1 {
            background-color: #D0F2E2;
            color: #2E6A4F;
            padding: 1rem;
            border-radius: 10px;
            font-style: italic;
            font-size: 2rem;
        }

        .card {
            border: 1px solid #8FC7A4;
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            background-color: #EAF8F1;
            text-align: center;
        }

        .card-img-top {
            border-bottom: 1px solid #8FC7A4;
            max-height: 200px;
            object-fit: cover;
        }

        .card-body {
            padding: 1.5rem;
        }

        .card-title,
        .card-text {
            color: #2E6A4F;
        }

        .btn-primary {
            background-color: #8FC7A4;
            border: none;
            border-radius: 10px;
        }

        .btn-primary:hover {
            background-color: #7DBF9A;
        }
    </style>
</head>

<body>
    <main class="d-flex flex-column align-items-center w-100 flex-grow-1">
        <h1 class="text-center w-100">Users</h1>
        <section class="text-center m-3 w-100 d-flex justify-content-center gap-3 flex-wrap">

            {{!-- {{#if user._id}} --}}
            <div class="card" style="width: 18rem;">
                <img src="{{user.photo}}" class="card-img-top" alt="{{user._id}}" />
                <div class="card-body">
                    <h5 class="card-title">{{user.email}}</h5>
                    <h5 class="m-2 p-2">Age: {{user.age}}</h5>
                    <div class="d-flex justify-content-around">
                        <strong class="card-text">Role: {{user.role}}</strong>
                    </div>
                </div>
            </div>
            {{!-- {{/if}} --}}
        </section>
    </main>
</body>

</html>