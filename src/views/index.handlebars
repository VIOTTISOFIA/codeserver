<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to The Baby's Shop</title>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous" />
    <style>
        body {
            font-family: 'Nunito', sans-serif;
            background-color: #F0F8F4;
            color: #333;
        }

        h1 {
            background-color: #DFF0D8;
            padding: 1rem;
            text-align: center;
            width: 100%;
            font-style: italic;
            color: #2E6A4F;
        }

        /* Estilo para el carrusel */
        .carousel {
            width: 100%;
            max-width: 800px;
            margin: 0 auto;
            position: relative;
        }

        .carousel-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 400px;
        }

        .carousel-item h2 {
            font-weight: bold;
            font-size: 1.2rem;
            color: #4CAF50;
            margin-bottom: 0.5rem;
        }

        .carousel-control-prev-icon,
        .carousel-control-next-icon {
            background-color: #4CAF50;
            width: 40px;
            height: 40px;
            border-radius: 50%;
        }

        .carousel-control-prev,
        .carousel-control-next {
            width: 5%;
            opacity: 0.8;
        }

        .carousel-control-prev:hover,
        .carousel-control-next:hover {
            opacity: 1;
        }

        /* Estilo para los botones de "See more" */
        .btn-primary {
      background-color: #4CAF50;
      /* Verde pastel */
      color: #FFFFFF;
      /* Texto blanco */
      border: none;
      border-radius: 8px;
      padding: 0.75rem 1.5rem;
      font-size: 16px;
      text-decoration: none;
      display: inline-block;
      transition: background-color 0.3s;
    }

    .btn-primary:hover {
      background-color: #45A049;
      /* Un verde ligeramente más oscuro para el hover */
    }

        /* Estilos para las tarjetas de productos */
        .product-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 1.5rem;
            justify-items: center;
            width: 100%;
        }

        .card {
            width: 100%;
            max-width: 300px;
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            background-color: #FFFFFF;
            border: 1px solid #DFF0D8;
            overflow: hidden;
        }

        .card-img-top {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }

        .card-title {
            font-size: 1.2rem;
            color: #4CAF50;
            text-align: center;
            font-weight: bold;
        }

        @media (max-width: 768px) {
            .product-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            .card {
                max-width: 100%;
            }
        }

        @media (max-width: 576px) {
            .product-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>

<body>
    <main>
        <h1>Welcome to The Baby's Shop</h1>

        <!-- Carrusel con Emoticonos -->
        <div id="featuresCarousel" class="carousel slide mb-4" data-bs-ride="carousel" data-bs-interval="3000">
            <div class="carousel-inner text-center">
                <div class="carousel-item active">
                    <span style="font-size: 3rem;">💳</span>
                    <h2>Formas de Pago</h2>
                </div>
                <div class="carousel-item">
                    <span style="font-size: 3rem;">🚚</span>
                    <h2>Envíos Gratis</h2>
                </div>
                <div class="carousel-item">
                    <span style="font-size: 3rem;">🛡️</span>
                    <h2>Garantías</h2>
                </div>
                <div class="carousel-item">
                    <span style="font-size: 3rem;">🏢</span>
                    <h2>Sucursales</h2>
                </div>
            </div>
            <button class="carousel-control-prev" type="button" data-bs-target="#featuresCarousel" data-bs-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#featuresCarousel" data-bs-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Next</span>
            </button>
        </div>

        <!-- Tarjetas de productos en una fila de 3 -->
        <section class="product-grid">
            {{#each products}}
            <div class="card">
                <img src="{{this.photo}}" class="card-img-top" alt="{{this.title}}">
                <div class="card-body">
                    <h5 class="card-title">{{this.title}}</h5>
                    <p class="card-text">Category: {{this.category}}</p>
                    <div class="d-flex justify-content-around">
                        <strong class="card-text">Price: {{this.price}}</strong>
                        <strong class="card-text">Stock: {{this.stock}}</strong>
                    </div>
                    <a href="/products/{{this._id}}" class="btn btn-primary mt-auto">See more</a>
                </div>
            </div>
            {{/each}}
        </section>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-VH2crvaS9dTj5SmUdcVknBlFHLiEYVHpT92Y/4SXiU9u4RY4TKegzB+GfD5yQg4k"
        crossorigin="anonymous"></script>
</body>

</html>